server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Miss1314
    url: jdbc:mysql://106.12.128.174:3306/spring_security?useUnicode=true&characterEncoding=utf8&autoReconnect=true&failOverReadOnly=false

logging:
  level:
    com:
      moon:
        springsecurity: debug
  file:
    name: log/logging.log
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
